---
layout: default.njk
permalink: "solutions.html"
renderData:
  title: "Solutions"
---
<h1 class="text-5xl">
  {{ renderData.title }}
</h1>

<p class="text-gray-600 mb-3 pt-12">
  Check out our current solutions. Don't see one that fits your business? <a class="underline" href="/contact.html"> Contact Us<a> today and let us get you connected with new local customers.
</p>

<div>
  <!-- component -->
  <div class="py-4">
    <div class="px-4">
      <div class="block md:flex justify-between md:-mx-2">
        {%- for solution in solutions -%}
        <div class="w-full lg:w-1/3 md:mx-2 mb-4 md:mb-0">
          <div class="bg-white rounded-lg overflow-hidden shadow relative">
            <img
              class="h-56 w-full object-cover object-center"
              src="{{ solution.image.src }}"
              alt="{{ solution.image.alt }}"
            />
            <div class="p-4 h-auto md:h-40 lg:h-48">
              <a
                href="{{ solution.url }}"
                rel="external"
                class="block text-blue-500 hover:text-blue-600 font-semibold mb-2 text-xl"
              >
                <h2>{{ solution.name }}</h2>
              </a>
              <div
                class="text-gray-600 text-sm leading-relaxed block md:text-xs lg:text-sm"
              >
               {{ solution.description }}
              </div>

              {# <div
                class="relative mt-2 lg:absolute bottom-0 mb-4 md:hidden lg:block"
              >
                 {%- for tag in solution.tags -%}
                    <span class="inline bg-gray-300 py-1 px-2 rounded-full text-xs lowercase text-gray-700 mr-2"
                  href="#"
                  >{{ tag }}</span>
                 {%- endfor -%}
                
              </div> #}
              <div class="pb-4 pt-4 ">
                <a href="{{ solution.url }}" class='bg-blue-500 text-white p-2 rounded text-xl font-bold text-right'>
                  {{ solution.button_text }}
                </a>
              </div>
            </div>
          </div>
        </div>
        {%- endfor -%}
      </div>
    </div>
  </div>
</div>
